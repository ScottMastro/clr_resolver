# Conda recreation recipe. clr_resolve resolves the minimap2 binary via
# config/tools.sh; this file is not consumed by --use-conda.
#   conda env create -f workflow/envs/minimap2.yaml
name: minimap2
channels:
  - conda-forge
  - bioconda
dependencies:
  - minimap2=2.28
